import { GetterTree } from 'vuex';
import { ResumoState } from './state';
import { RootState } from '@/store';

export enum ResumoGetterTypes {

}

const getters: GetterTree<ResumoState, RootState> = {

};

export default getters;
